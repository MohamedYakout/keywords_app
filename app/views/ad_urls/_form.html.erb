<%= form_for(@ad_url) do |f| %>
  <% if @ad_url.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ad_url.errors.count, "error") %> prohibited this ad_url from being saved:</h2>

      <ul>
      <% @ad_url.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :link %><br>
    <%= f.text_field :link %>
  </div>
  <div class="field">
    <%= f.label :position %><br>
    <%= f.text_field :position %>
  </div>
  <div class="field">
    <%= f.label :is_ad %><br>
    <%= f.check_box :is_ad %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
